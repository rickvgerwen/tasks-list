import { HighlightFilterPipe } from './highlight-filter.pipe';

describe('HighlightFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
